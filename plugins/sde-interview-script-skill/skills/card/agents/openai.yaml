interface:
  display_name: "Card"
  short_description: "Turn text into Excalidraw script cards"
  default_prompt: "Use $card to turn this text into an Excalidraw-style preview image and link only. Default to English unless I specify another language."
